{
  "name": "susu-protocol",
  "version": "1.0.0",
  "private": true,
  "description": "Decentralized rotating savings protocol on Polkadot Hub EVM",
  "scripts": {
    "contracts:compile": "pnpm --filter contracts compile",
    "contracts:test": "pnpm --filter contracts test",
    "contracts:deploy": "pnpm --filter contracts deploy:westend",
    "frontend:dev": "pnpm --filter frontend dev",
    "frontend:build": "pnpm --filter frontend build",
    "dev": "pnpm --filter frontend dev"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
